version: '3.9'
services:
  gateway:
    build: ./gateway
    ports:
      - "8080:8080"
    depends_on:
      - auth
      - api
      - socket
  auth:
    image: nginx:alpine  # placeholder
  api:
    image: nginx:alpine  # placeholder
  socket:
    image: nginx:alpine  # placeholder
